{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Home</a></li>
        <li class="breadcrumb-item active">Articles</li>
    </ol>
{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-body">

            {{ form_start(form) }}
            <div class="row">
                <div class="col-md-2">{{ form_widget(form.name) }}</div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary">Filter</button>
                    <a href="{{ path('articles') }}" class="btn btn-outline-secondary">Reset</a>
                </div>
                <div class="col-md-2">
                    <a href="{{ path('articles.create') }}" class="btn btn-outline-primary">Create</a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>

    <div class="box">
        <table class="table table-bordered table-hover">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'Name', 'name') }}</th>
            </tr>
            </thead>
            <tbody>
            {% for article in pagination %}
                <tr>
                    <td><a href="{{ path('articles.show', {'id': article.id}) }}">{{ article.name }}</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

{% endblock %}